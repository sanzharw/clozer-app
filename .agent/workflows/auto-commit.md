---
description: Always commit and push changes to GitHub after completing edits
---

After completing any code changes:

// turbo
1. Stage, commit, and push all changes:
```bash
git add -A && git commit -m "<descriptive commit message>" && git push
```

Use a concise, conventional commit message (e.g. `fix:`, `feat:`, `refactor:`, `chore:`).
